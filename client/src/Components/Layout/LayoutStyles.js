import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  layoutContainer: {
    display: 'flex',
  },
  allLayoutMargins: {
    margin: '80px 20px',
  },
  headerLinkText: {
    cursor: 'pointer',
  },
}));

export default useStyles;
